<?php

use Artax\Parsing\MessageParser;

class MessageParserTest extends PHPUnit_Framework_TestCase {
    
    function testFixtureResponseParse() {
        $this->markTestSkipped();
        /*
        $file = '/home/daniel/dev/php/Artax/test/fixture/raw_responses/espn.go.com.txt';
        $msg = file_get_contents($file);
        
        $parser = new MessageParser(MessageParser::MODE_RESPONSE);
        
        //for ($i=0; $i<strlen($msg); $i++) {
        //    $result = $parser->parse($msg[$i]);
        //}
        $result = $parser->parse($msg);
        */
    }
    
}

